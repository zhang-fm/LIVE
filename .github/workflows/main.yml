name: IPTV详情自动抓取

on:
  workflow_dispatch: # 手动触发

jobs:
  run_crawler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
         pip install playwright==1.40.0 playwright-stealth==1.0.6
         playwright install chromium
         playwright install-deps chromium

      - name: 🚀 执行模拟点击抓取
        run: python py/zubobsk.py
        env:
          PYTHONUNBUFFERED: "1"

      - name: 上传抓取结果 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: crawl-results
          path: |
            detail_page.html
            error.png
