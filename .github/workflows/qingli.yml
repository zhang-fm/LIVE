name: 每日仓库例行维护 (清理失效源)

on:
  schedule:
    # 每天凌晨 04:00 UTC (北京时间 12:00) 运行
    - cron: '0 4 * * *'
  workflow_dispatch: # 支持手动触发

jobs:
  maintenance:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: pip install requests

      - name: 1. 清理失效酒店源 (Hotel)
        run: python py/hotelqingli.py

      - name: 2. 清理失效组播源 (Zubo)
        run: python py/zubo_cleanup.py

      - name: 4. 提交更改并推送
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 关键修改：使用 -A 参数，它会记录文件的增加、修改以及【删除】
          git add -A
          
          # 检查是否有变动（包括删除）
          if git diff --staged --quiet; then
            echo "✅ 仓库已经是最新状态，无需清理。"
          else
            git commit -m "🧹 每日维护：清理失效源并同步删除记录"
            git pull origin main --rebase
            git push origin main
            echo "🚀 清理成功，文件已从仓库中移除。"
          fi
