name: 每日仓库例行维护 (清理失效源)

on:
  schedule:
    # 每天凌晨 04:00 UTC (北京时间 12:00) 运行
    - cron: '0 4 * * *'
  workflow_dispatch: # 支持手动触发

jobs:
  maintenance:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: pip install requests

      - name: 1. 清理失效酒店源 (Hotel)
        run: python py/hotelqingli.py

      - name: 2. 清理失效组播源 (Zubo)
        run: python py/zubo_cleanup.py

      - name: 3. 提交更改并推送
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 添加所有可能变动的目录
          git add hotel/*.m3u
          git add hotel/hotel_history.txt || true
          git add zubo/*.m3u
          git add *.m3u || true
          
          if git diff --staged --quiet; then
            echo "✅ 检查完毕，没有发现需要清理的失效源。"
          else
            git commit -m "🧹 每日例行维护：清理失效酒店源与组播源"
            git pull origin main --rebase
            git push origin main
            echo "🚀 清理完成，已推送到仓库。"
          fi
